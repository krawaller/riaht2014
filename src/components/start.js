/** @jsx React.DOM */

var React = require('react');

var Start = React.createClass({
  render: function() {
    return (
      <p>Welcome to the JavaScript Guild!</p>
    );
  }
});

module.exports = Start;